"""Smoke test that proves the test harness runs.

Replaced by real coverage as the platform grows; for now it exists so the CI
`pytest` step has something to collect and exits green.
"""


def test_harness_runs() -> None:
    assert True
