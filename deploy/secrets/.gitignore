# Never commit an age private key, even if it lands here by mistake.
keys.txt
*.agekey
age-key*

# The encrypted secrets.sops.yaml IS committed (sops ciphertext, key offline).
# A pre-commit hook (scripts/check-sops-encrypted.sh) refuses a plaintext one.
